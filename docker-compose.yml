version: '3.8'

services:
  qdml-api:
    build: ./core
    ports:
      - "8001:8001"
    environment:
      DATABASE_URL: postgresql://ai_auto:233f290cb68a514e3bb740d134f5bd50@host.docker.internal:5432/ai_auto
      QDML_SCHEMA: qdml
      HOST: 0.0.0.0
      PORT: 8001
    volumes:
      - ./mas-front/_generated:/app/static
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
